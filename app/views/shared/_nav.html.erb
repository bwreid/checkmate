<nav class="top-bar">
  <ul class="title-area">
    <!-- Title Area -->
    <li class="name">
      <h1><%= link_to("Checkmate", root_path) %></h1>
    </li>
    <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Left Nav Section -->
    <% if @auth.nil? %>
    <ul class="left">

      <li><%= link_to("Create Account", new_user_path, :remote => true ) %>

      <li><%= link_to("Login", login_path, :remote => true ) %>

    </ul>
    <% end %>

    <!-- Right Nav Section -->
    <ul class="right">
      <% if @auth %>

      <li><%= link_to("#{ @auth.username }", '#' ) %>

      <li><%= link_to("Logout", login_path, :method => 'delete', :remote => true ) %>

      <% end %>
    </ul>
  </section>
</nav>